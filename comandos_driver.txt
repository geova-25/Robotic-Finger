make -C /usr/src/linux-headers-$(uname -r) M=$(pwd modules)
sudo mknod /dev/finger c 60 0
sudo chmod 666 /dev/finger
sudo insmod finger.ko
echo -n H >/dev/finger
cat /dev/finger

lsmod
sudo rmmod finger
